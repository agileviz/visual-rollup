{
  "name": "visual-rollup",
  "version": "0.9.7",
  "description": "Parent work-item progress, rolled up from children's states — driven by any shared query.",
  "keywords": [
    "Azure DevOps",
    "Azure Boards",
    "Dashboard",
    "Widget",
    "Rollup",
    "Work Item",
    "Hierarchy"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/agileviz/visual-rollup.git"
  },
  "scripts": {
    "clean": "rm -rf ./dist",
    "compile": "npm run clean && webpack --mode production",
    "compile:dev": "npm run clean && webpack --mode development",
    "lint": "eslint `src/**/*.{ts,tsx}`",
    "test": "jest --coverage",
    "test-watch": "jest --watchAll --coverage",
    "serve": "webpack serve --mode development",
    "build": "rm -f ./*.vsix && npm run compile",
    "build:dev": "rm -f ./*-dev-*.vsix && npm run compile:dev",
    "sync-version": "node scripts/sync-version.js",
    "postbuild": "npm run package-extension",
    "postbuild:dev": "npm run package-extension-dev -- --overrides-file extension-DEV.json",
    "package-extension": "tfx extension create --manifest-globs extension.json src/Contribs/**/*.json",
    "package-extension-dev": "tfx extension create --manifest-globs extension.json src/Contribs/Config/Config.json src/ContribsDev/Widget/Widget.json",
    "publish-extension": "tfx extension publish --manifest-globs extension.json src/Contribs/**/*.json --rev-version --service-url https://marketplace.visualstudio.com --token $ADO_PUBLISH_TOKEN && npm run sync-version",
    "publish-extension-dev": "tfx extension publish --manifest-globs extension.json src/Contribs/Config/Config.json src/ContribsDev/Widget/Widget.json --overrides-file extension-DEV.json --rev-version --token $ADO_PUBLISH_TOKEN && npm run sync-version",
    "tfx-help": "tfx extension create --help",
    "npm-check-minor-updates": "ncu --target minor",
    "npm-do-minor-updates": "ncu -u --target minor",
    "npm-check-latest-updates": "ncu --target latest",
    "npm-do-latest-updates": "ncu -u --target latest"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.238.0",
    "azure-devops-extension-sdk": "^4.0.2",
    "azure-devops-ui": "^2.241.0",
    "normalize.css": "^8.0.1",
    "slim-select": "^3.4.3"
  },
  "overrides": {
    "sockjs": {
      "uuid": ">=11.1.1"
    },
    "webpack-dev-server": {
      "http-proxy-middleware": ">=3.0.6"
    },
    "shell-quote": ">=1.8.4",
    "lodash": ">=4.18.1",
    "@istanbuljs/load-nyc-config": {
      "js-yaml": ">=4.2.0"
    }
  },
  "devDependencies": {
    "@eslint/js": "^9.5.0",
    "@types/eslint__js": "^8.42.3",
    "@types/jest": "^29.5.12",
    "copy-webpack-plugin": "^14.0.0",
    "css-loader": "^7.1.2",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "npm-check-updates": "^21.0.2",
    "sass": "^1.77.5",
    "sass-loader": "^17.0.0",
    "style-loader": "^3.3.4",
    "tfx-cli": "^0.23.1",
    "ts-jest": "^29.1.4",
    "ts-loader": "^9.6.2",
    "typescript": "~5.9.3",
    "typescript-eslint": "^7.13.0",
    "webpack": "^5.108.1",
    "webpack-cli": "^7.1.0",
    "webpack-dev-server": "^5.2.4"
  }
}
